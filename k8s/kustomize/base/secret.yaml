apiVersion: v1
kind: Secret
metadata:
  name: python-project-secrets
type: Opaque
# Values are intentionally empty here.
# Each overlay supplies real values via secretGenerator or an external patch.
# Never commit real secrets to this file.
stringData:
  EXAMPLE_VARIABLE_NAME: ""
