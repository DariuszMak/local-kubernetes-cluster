# Do NOT commit real secrets. This is a template.
# Generate the real secret with the helper script: scripts/k8s-apply-secrets.ps1
apiVersion: v1
kind: Secret
metadata:
  name: python-project-secrets
type: Opaque
stringData:
  EXAMPLE_VARIABLE_NAME: "Hi it is me!"
  # Add more keys here matching your .dev.env
